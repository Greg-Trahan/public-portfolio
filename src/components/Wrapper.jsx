const Wrapper = ({children}) => {

  return (
    <div style={{ height: "100vh", backgroundColor: "#000033", color: "#cccccc", marginTop:"1em", padding:"2em"}}>
      { children }
    </div>
  )
}

export default Wrapper


